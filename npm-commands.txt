npm start -- starts development server on http://localhost:3000/
npm build -- statically generate /build folder
or
npm run-script build


--- Commit to git and already deploy to production radubulai.com ---

git status
git add -A
git commit -m "Message"
git push -u origin master
npm run deploy